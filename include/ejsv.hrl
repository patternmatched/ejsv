-record(keyword, { name,
                   assertion,
                   params,
                   path }).

-record(schema, { id,
                  keywords,
                  transform,
                  type,
                  version }).
